#!/usr/bin/env node
/**
 * UTGÅTT (2026-07-25): hex-gaten er avviklet — den gamle designkanonen er tømt
 * mens nytt designsystem utvikles i Open Design. Denne filen er en bevisst
 * NO-OP inntil `.github/workflows/ci.yml` kan oppdateres (workflow-endringer
 * krever scope som integrasjonen ikke har). Slett scriptet, `check:hex` i
 * package.json og CI-steget i samme commit når det er mulig.
 */
console.log("check:hex er utgått (design-gate fjernet 2026-07-25) — gjør ingenting.");
process.exit(0);
