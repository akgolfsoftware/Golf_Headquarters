/**
 * AK Golf HQ — Intern design-lab (Fase F).
 * URL: /design-system
 * Viser v2 core + tema-bryter mot Open Design-fasit.
 * AgencyOS-terminologi — aldri CoachHQ.
 */

import { DesignLabV2 } from "@/components/v2/design-lab-v2";

export const metadata = {
  title: "Design-lab · AK Golf HQ (intern)",
};

export default function DesignSystemPage() {
  return <DesignLabV2 />;
}
