/**
 * Varsler-layout. Ingen fane-nav (SubNav) — fasiten (VarslerScreen) er én
 * selvstendig side uten faner.
 */
export default function VarslerLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
