/* Skeleton for Mål-hub og underruter (runder/trackman) uten egen loading.tsx
   (Next.js nærmeste-ancestor-mønster) — kort-grid speiler MalHubV2. */

import { V2Laster } from "@/components/v2/feil-laste";

export default function Loading() {
  return <V2Laster variant="kort" />;
}
