/* v2-skjelett for /portal/meg (B1 «Meg laster»): avatar + navn + kort. */

import { V2Laster } from "@/components/v2/laster";

export default function Loading() {
  return <V2Laster variant="meg" />;
}
