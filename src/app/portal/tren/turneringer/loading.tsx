/* Laster-tilstand for /portal/tren/turneringer (Paper-port W1, fase2).
   Fasit: playerhq-turneringer.html — enkle skjelettlinjer i avtagende bredde. */

import { V2Laster } from "@/components/v2/laster";

export default function Loading() {
  return <V2Laster variant="liste" />;
}
