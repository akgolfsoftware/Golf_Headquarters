/**
 * Workbench Hybrid — den delte planleggings-kjernen (PlayerHQ + AgencyOS).
 * Faser 1–3 (shell, Uke/Dag + palette-DnD, AK-formel-inspektør). Resten er stub.
 * Orkestratoren kobler rutene etter review.
 */

export { WorkbenchHybrid } from "./WorkbenchHybrid";
export type { WorkbenchHybridProps } from "./WorkbenchHybrid";
export type { WorkbenchRole } from "./types";
