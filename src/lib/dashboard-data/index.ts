/**
 * Read-only lesemodul mot `dashboard`-schemaet (DataGolf-data i akgolf-hq sin
 * egen Supabase-database). Se `queries.ts` for detaljer og `schemas.ts` for
 * zod-valideringen. Ikke koblet til noen skjerm ennå (kommer i N12).
 */
export * from "./schemas";
export * from "./queries";
