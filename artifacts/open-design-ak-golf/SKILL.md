---
name: ak-golf-academy
description: Athletic intelligence design system for AK Golf Academy — AgencyOS, PlayerHQ, public site and social. Use before any HTML. Do not invent palette, type, nav or screens that belong in a separate project.
---

# Skill — AK Golf Academy

Read `DESIGN.md` and `tokens.css` before writing a line.

**Decide the half first.**

1. **App** (AgencyOS, PlayerHQ, AK Golf HQ) — `DESIGN.md` law only. No photography as background. No marketing components. No `--fs-mkt-*`.
2. **Web / social** — same palette + photo on scrim, graphite bands, rust once.

Always: Norwegian copy, no emoji, status as a sentence, `—` never `0`, lagret ≠ delt, human approves.

If the user asks to “design the whole app” inside this system: refuse. Build **empty shells** (AgencyOS 1440/834/390, PlayerHQ 390) only. Real screens go in a **new Open Design project** that selects this system.

Logos: `assets/logos/`. Never redraw. Never invert colour lockups.
