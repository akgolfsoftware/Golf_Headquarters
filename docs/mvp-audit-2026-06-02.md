# MVP-audit — AK Golf HQ — 2026-06-02

Komplett gjennomgang for MVP-lansering. Utført av Claude Code (full kodebase-audit, 4 parallelle agenter + build/tsc-verifikasjon).

## Helsestatus (verifisert)

| Sjekk | Resultat |
|---|---|
| `tsc --noEmit` | ✅ Exit 0 — ingen TypeScript-feil |
| `npm run build` | ✅ Compiled successfully in 29.7s |
| Kodebase | 1719 TS/TSX-filer, 414 page.tsx-ruter |
| `_archive/` (2347 md) | Gitignored — lokal støy, ikke i repo |

Build og typer er grønne. Hovedrisikoen er ikke kompilering, men **stub-funksjoner som ser ferdige ut men ikke gjør noe**, og **rot/duplikater** som gjør det vanskelig å vite hva som er ekte.

---

## DEL 1 — Kritiske funn (MÅ fikses før lansering)

### K1 — Fake Stripe-betalingsflyt (KRITISK, verifisert)
`src/app/portal/meg/abonnement/kort/ny/kort-form.tsx` — `handleSubmit` kjører to `setTimeout` som simulerer SUCCESS uten noe Stripe-kall (linje 50-53). Brukere tror de legger til betalingskort. **Ingen betaling skjer.** Enten koble til ekte Stripe Elements, eller skjul siden bak feature-flag til den virker.

### K2 — Hardkodede passord i git (KRITISK, verifisert)
`scripts/reset-auth-passwords.ts` — klartekst-passord for 4 navngitte brukere committet til repo. Fjern fra kode (les fra env) + roter passordene (de ligger fortsatt i git-historikk).

### K3 — Onboarding (LØST 2026-06-02 — var feildiagnostisert)
Ved nærmere undersøkelse: den EKTE onboarding-flyten `/auth/onboarding` (dit signup
redirecter) lagrer korrekt til strukturerte User-kolonner (hcp, playingYears, ambition,
homeClub, dateOfBirth, name) + håndterer GDPR-foreldresamtykke. Klubb-onboarding
(`onboard/klubb/actions.ts`) lagrer også til `User.preferences.klubbOnboarding`.
Den flaggede stuben lå i `/onboarding` — en FORLATT DUPLIKAT (ingen lenker dit,
hardkodet testnavn). Fjernet. Ingen migrasjon nødvendig — feltene fantes allerede.

### K4 — `http://localhost` hardkodet i prod OAuth (KRITISK)
`src/app/api/google-calendar/connect/route.ts:15` — redirect bruker `http://localhost` som base. Gir feil redirect i produksjon.

### K5 — Ubeskyttede demo-ruter i prod (HØY)
`/forhandsvisning` og `/workbench-preview` mangler auth-gate (ikke i `DEMO_PREFIXES` i proxy.ts), har hardkodet mock-data ("Magnus Rolfsen"). Begge er kommentert "fjernes ved lansering". Slett dem.

### K6 — CRON_SECRET mangler fail-fast (HØY)
`src/lib/env.ts` validerer kun 5 vars. Mangler `CRON_SECRET` → alle 18 cron-jobber avvises stille i prod. Legg til i kritisk-listen + i `.env.example`.

### K7 — Destruktivt script uten guard (HØY)
`scripts/cleanup-tournaments.ts` kaller `deleteMany()` uten `--dry-run` eller prod-sjekk. Kan slette alle turneringer ved uhell.

### K8 — Notion OAuth lagrer aldri token (HØY)
`src/app/api/notion/oauth/callback/route.ts:113` — mottar access_token, logger, kaster. Integrasjonen er en dead-end. Fiks eller deaktiver endepunktet.

### K9 — Manglende authz på coach-handling (HØY)
`src/components/coachhq/workbench/actions.ts:52` — TODO: ingen sjekk på at coach eier spilleren. Coach kan sende Caddie-melding for fremmed spiller.

### K10 — `@ts-expect-error` på ikke-eksisterende Prisma-modeller (HØY)
`CoachDrillDirectiv`, `DrillMestringsLogg`, `DrillRating` brukes i aktive actions (`admin/spillere/[id]/actions.ts`, `portal/drills/[id]/actions.ts`) men finnes ikke i schema → krasjer runtime hvis kalt. Enten legg til modellene eller deaktiver knappene.

### Andre prod-stubs (skjul eller fiks)
- `src/lib/push.ts:67` — push-varsler returnerer alltid `{ ok: false }`.
- `/api/recording/dummy-transcript` — test-endepunkt merket "skal fjernes", finnes i prod.
- `/portal/(fullscreen)/test/[testId]/live` + `summary` — merket "pixel-perfekt mockup".
- Google Calendar-tokens utløpt (`invalid_grant` × mange i build-logg) — reconnect før lansering.
- `src/proxy.ts:70` — hardkodet Supabase prosjekt-ID i CSP (bruk env).

---

## DEL 2 — Duplikater (konsolider)

### Komponent-duplikater
| Duplikat | Anbefaling |
|---|---|
| `shared/empty-state.tsx` (65 bruk) vs `ui/empty-state.tsx` (5 bruk) | Migrer de 5 til shared, slett ui-versjonen |
| `shared/profil-rediger-modal.tsx` vs `meg/profil-rediger-modal.tsx` | Behold én, slett den andre |
| `shared/view-mode-toggle.tsx` vs `shared/ViewModeToggle.tsx` | Forvirrende identiske navn — døp om / slå sammen |
| `shared/sheet.tsx` vs `ui/sheet.tsx` | Bruk ui/ (shadcn), slett shared |
| 2-versjon-par: `topbar`, `tabs`, `bottom-nav`, `caddie-chat`, `eyebrow`, `live-bar`, `plan-card`, `section-header`, `icon` (×4), `sidebar` (×7) | Kartlegg hvilken som er i bruk, slett resten — post-launch ok |

### Rute-duplikater (halvferdige migrasjoner)
| Gammel | Ny (canonical) | Handling |
|---|---|---|
| `admin/calendar` | `admin/kalender` | Migrer `calendar-view-toggle.tsx`, slett `calendar/` |
| `portal/analyse` | `portal/analysere` | Oppdater `global-search-modal`, slett `analyse/` |
| `admin/approvals` | `admin/godkjenninger` | Velg én URL, fiks `hub-kpi-strip.tsx` |
| `admin/locations`+`facilities` | `admin/anlegg` | Fiks lenke i `anlegg-grid.tsx`, slett gamle |
| `portal/stats/*` vs `portal/statistikk` | KREVER BESLUTNING | Avklar hvilken som er canonical |
| `portal/meg/sikkerhet` vs `portal/meg/innstillinger/sikkerhet` | KREVER BESLUTNING | Konsolider til én |

### Dead code (~25 filer, ~6500 linjer — ingen imports)
`portal-analysere/` (7), `portal-dashboard/` (3), `admin-analyse-v2/` (2), `admin-anlegg-v2/` (1), `test-modul-v2/` (7), `planlegge-v2/` (5), `coachhq-stubs/`, `styles/v2/patterns.css`. Slett.

---

## DEL 3 — Demo/preview-ruter (slett før lansering)

Auth-gated men overflødige (produksjonsversjon finnes):
`hull-demo`, `kalender-demo`, `kalender-maaned-demo`, `lokasjoner-demo`, `sesjon-opptak-demo`, `talent-kohort-demo`, `talent-region-pipeline-demo`, `talent-sammenlign-to-demo`, `talent-spiller-360-demo`, `v2-preview`, `(internal)/design-system-v2`.

Ubeskyttet — slett straks: `forhandsvisning`, `workbench-preview`.

Krever beslutning (behold bak intern tilgang?): `/demo` (236-siders indeks), `portal-preview`, `coach-preview`, `intern/komponenter/*`.

---

## DEL 4 — Dokumentasjon/planfiler (rydd)

### README.md — SKRIV OM
Standard create-next-app boilerplate. Ubrukelig. Bør ha: prosjektbeskrivelse, lenke til PLATFORM.md, env-oversikt, dev-setup.

### Behold (8 viktigste)
`CLAUDE.md`, `PLATFORM.md`, `LAUNCH-CHECKLIST.md`, `docs/go-live-sjekkliste.md`, `docs/todo.md`, `docs/konsolidert-plan-2026-06-02.md`, `docs/gdpr-behandlingsregister.md`, `docs/runbook.md` + `rollback.md` + `sla.md`.

### Flytt til _archive/ (~185 filer)
- Root: `PIXEL-PERFECT-SCREEN-MAPPING.md`, `audit-rapport-2026-05-26.md`
- Datostemplede engangsplaner: alle `docs/2026-05-27-*.md`, `docs/*-2026-06-01.md` (unntatt aktive), `backlog-*.md`, `handover-*.md`
- Security-audit-trio (alle 3 — `SECURITY.md` er levende kilde)
- Design-audit-duplikater (2-3 filer)
- `docs/audit/` (7 filer), `docs/superpowers/plans/2026-05-*` (fullførte)
- `docs/migration/PROJECT-V2-MIGRATION-PLAN.md` (v1)
- `docs/launch-checklist.md` (gammel, erstattet)
- `docs/design-resources/brand-references/` (142 filer inspirasjonskatalog)

---

## DEL 5 — Forenklet MVP-plan (kritisk vei)

MVP = de 4 produktene fungerer ende-til-ende for ekte brukere:

1. **Auth + onboarding lagrer faktisk data** (K3) — uten dette har du ingen brukere.
2. **Betaling virker eller er skjult** (K1) — ikke lur brukere med fake kortskjema.
3. **Booking-flyten ende-til-ende** — verifiser mot LAUNCH-CHECKLIST.
4. **Sikkerhet ren** (K2, K6, K7, K9) — ingen hardkodede passord, cron virker, authz på plass.
5. **Skjul/fjern alt som er mockup** (demo-ruter, dummy-transcript, push, test-mockups).
6. **Integrasjoner: fiks eller deaktiver** (Google Calendar reconnect, Notion OAuth).

Alt annet (Elite Fase 2, drill-modeller, dead code-rydding, komponent-dedup) er **post-launch**.

---

## DEL 6 — Handlingsplan

### I dag (kritisk sikkerhet + rydding — lav risiko)
1. Slett `scripts/reset-auth-passwords.ts` + roter de 4 passordene (K2).
2. Slett ubeskyttede demo-ruter: `forhandsvisning`, `workbench-preview` (K5).
3. Legg `CRON_SECRET` (+ booking/anthropic/encryption-keys) i `env.ts` + `.env.example` (K6).
4. Legg `--dry-run`/prod-guard i `cleanup-tournaments.ts` (K7).
5. Flytt ~185 utgåtte docs til `_archive/`. Skriv om README.md.
6. Slett ~25 dead-code-filer (ingen imports — trygt).

### Neste 1-2 dager (funksjonell MVP)
7. Fiks onboarding-persistering (K3) — completeOnboarding + saveKlubb til DB.
8. Beslutt Stripe: koble til ekte Elements ELLER feature-flag bort kortskjema (K1).
9. Fiks `http://localhost` OAuth-redirect (K4).
10. Notion OAuth: lagre token eller deaktiver endepunkt (K8).
11. Authz-sjekk på coach-handlinger (K9).
12. Fiks `@ts-expect-error`-modeller: legg til i Prisma eller skjul knappene (K10).
13. Konsolider rute-duplikater (calendar/kalender, analyse/analysere, approvals/godkjenninger, locations/anlegg).
14. Reconnect Google Calendar-tokens.

### Post-launch
Komponent-dedup, `portal/stats`-beslutning, sikkerhets-side ("kommer snart"), Elite Fase 2.

---

## Team-oppsummering (del denne)

> **AK Golf HQ — MVP-status 2026-06-02.** Build og typer er grønne. Plattformen er nær lansering, men har 10 kritiske funn å lukke først: en fake Stripe-betalingsflyt som lurer brukere, hardkodede passord i git, onboarding som ikke lagrer data, en hardkodet localhost-URL i prod, og to ubeskyttede demo-ruter. I tillegg ligger ~185 utgåtte planfiler og ~6500 linjer død kode i veien, samt fem halvferdige rute-migrasjoner (calendar/kalender m.fl.). Plan: dag 1 = sikkerhet + rydding (lav risiko), dag 2-3 = gjør de 4 produktene funksjonelle ende-til-ende (onboarding, betaling, auth, integrasjoner). Alt annet er post-launch.
