# Feillogg

Format: `<dato> | <hva gikk galt/kostet tid> | <rotårsak> | <regel som hindrer gjentakelse>`
Ingen feil i økten: `<dato> | ren økt`

Én linje per økt der noe kostet ekstra tid. Lagt til av `/pr` ved behov — se `.claude/commands/pr.md`.

2026-08-29 | PX-7 (`px/7-tilstander-brekk`): `npm run build`/`check-critical-imports.mjs` feiler i denne worktreen med «Could not find the Next.js package» / «BUNDLE FAIL … undefined» selv om alt annet (tsc, eslint, token-gap, action-auth, doc-lenker, `npm test` — 1848/1848) er grønt | Worktreen (`.claude/worktrees/<id>`) har INGEN egen `node_modules` — den ligger kun i hovedsjekkuten `/home/user/Golf_Headquarters/node_modules`. Vanlig Node-modulresolusjon (tsc/eslint/prisma via npx) går fint fordi den vandrer oppover katalogtreet og finner hoved-node_modules siden worktreen er nøstet UNDER repo-roten — men Turbopack (`next build`, egen root-deteksjon) og `check-critical-imports.mjs` (bygger literal `path.join(process.cwd(), "node_modules", "esbuild", "bin", "esbuild")`) gjør IKKE den oppover-vandringen, og feiler | Samme klasse som PR #275-begrensningen i gotchas.md («kan ikke DB-verifiseres i worktreen»), nå bekreftet for build/critical-imports også: i en nøstet worktree uten egen `node_modules`, stol på tsc+eslint+token-gap+action-auth+doc-lenker+`npm test` lokalt og la CI (som kjører i hoved-repoet) dekke build/critical-imports/screenshot-gaten

2026-08-16 | Jarvis-porten (PR #532): kjørte tsc/eslint/build/test hver for seg lokalt, alt grønt — men CI sin `verify`-jobb feilte likevel på `check:token-gap` (to rå `rgba(0,0,0,…)` i MegPalett.tsx). Kostet en ekstra push-runde | `npm run verify` i `package.json` kjører FAKTISK `check-token-gap` + `check-critical-imports` i tillegg til tsc/eslint/build — men CLAUDE.md sin prosebeskrivelse av `verify` i denne øktens systemprompt nevnte dem ikke, så jeg satte sammen gate-lista fra hukommelsen/dokumentasjonen i stedet for å bare kjøre selve scriptet. Nøyaktig samme feilklasse er allerede logget 2026-08-15 (WANG-redesign) med samme remedy — jeg fulgte ikke den loggførte regelen | Kjør `npm run verify` som ÉN kommando (les den fra `package.json`, ikke fra en prosebeskrivelse i CLAUDE.md/systemprompt) tidlig i enhver jobb med UI-endringer — ikke sett sammen gate-lista fra hukommelsen. CLAUDE.md sin `verify`-beskrivelse bør også oppdateres til å nevne check-token-gap/check-critical-imports eksplisitt

2026-08-16 | Workflow med 7 parallelle lese-subagenter i remote-økt: alle 7 feilet uten å få lest én fil — ~537k tokens brukt på ingenting | Permission-handleren i subagent-miljøet (canUseTool/PermissionRequest i remote-harnesset) returnerte `updatedInput` som strippet ALLE parametre fra hvert verktøykall (Read/Bash/Grep/Glob), så alt feilet på schema-validering før filsystemet ble nådd. Hovedøktens egne verktøykall virket hele tiden — feilen ligger i remote-miljøets subagent-oppsett, ikke i repoet eller `beskytt.mjs` | I remote-økter: test med ÉN billig subagent at verktøykall går gjennom FØR en flåte startes; feiler den med «updatedInput … failed schema validation», gjør lesingen selv i hovedøkten (small docs: Read direkte + grep-telling er raskere enn orkestrering uansett)

2026-08-16 | Skrev i en commit-melding at `tittel` på den åpne `/team-wang` lekket elevnavn, og at det gjensto en datavask i kalenderen. Var i ferd med å utvide auth-sperren til hele `/team-wang` — som hadde stengt en side som med vilje skal kunne deles med elever og foreldre — før jeg etterprøvde. Null elevnavn i titlene: eneste personnavn var den eksterne testeren Teemu Paasanen, og «Fredrik»-treffene var 54× «Fredrikstad» | Jeg fant navn i `beskrivelse`, målte den lekkasjen ordentlig, og lot deretter mistanken smitte over på nabofeltet uten å måle det. Mønstergjenkjenning («fritekst = fritekst») erstattet observasjon i akkurat det ene steget som skulle begrunne en tilgangsendring | Mål hvert felt for seg. Én bekreftet lekkasje i felt A er ingen evidens for felt B, selv når feltene har samme opphav. Og: en påstand som skal begrunne at noe SPERRES må etterprøves før tiltaket, ikke etterpå — det billigste tidspunktet å oppdage at premisset er feil, er før koden er skrevet. Etterprøvingen her tok ett curl-kall

2026-08-15 | Prod-røyktesten sto rød i 195 kjøringer på rad i ni dager (06.08–15.08) uten at noen fant årsaken, og oppgaven ble beskrevet som «feilet siden 15.08 kl. 08:38» — ni dager for sent | `portal-analysere.visual.spec.ts` manglet skip-vakten de 104 andre paper-visual-spec-ene har, navigerte uinnlogget til en auth-beskyttet rute og lette etter et referansebilde som bare fantes som `-chromium-darwin` mens CI kjører ubuntu. To dokumenter (`.gitignore` + `tests/e2e/paper-visual/README.md`) påsto at snapshot-testene «aldri kjørte i CI» — de gjorde det, og påstanden er trolig grunnen til at ingen lette der | Når en test-familie ikke skal kjøre i CI, håndhev det ETT sted strukturelt (`testIgnore` i `playwright.config.ts`), ikke med en vakt hver enkelt fil kan glemme. Og: sjekk `gh run list --workflow=<x> --json conclusion` for SISTE GRØNNE kjøring før du tror på når noe begynte å feile — den observerte datoen er nesten alltid nyere enn den ekte

2026-08-15 | WANG-redesign: timeplanen for mobil dukket opp på DESKTOP også, og `npm run verify` feilet på slutten av en ellers ferdig jobb fordi `check-token-gap` fanget to inline `rgba()` | To ting: (1) `.wang-vis-mobil` skjulte med `display: none` UTEN `!important`, mens søsteren `.wang-vis-desktop` hadde det — en inline `display: flex` på samme element slo derfor ut skjulingen én vei men ikke den andre. (2) Jeg kjørte `tsc` + `eslint` løpende, men ikke hele `verify`, så en gate jeg ikke visste om slo først til slutt | En responsiv av/på-bryter må ha `!important` i BEGGE retninger, og layouten settes på et barn — aldri på selve bryteren. Og: kjør `npm run verify` ÉN gang tidlig i en lang jobb for å lære hvilke gates som finnes, ikke bare til slutt

2026-08-15 | D3-digesten viste «10. august – 17. august» (åtte dager), og ukespørringene dro med seg mandagens økter i to uker | `endOfWeek` i `src/lib/uke-helpers.ts` returnerer MANDAG NESTE UKE kl. 00:00 — en EKSKLUSIV øvre grense. Jeg leste navnet som «siste øyeblikk i uka» og brukte den med `lte` | `endOfWeek` brukes med `lt`, aldri `lte`; til visning trekk fra ett døgn. Les alltid returverdien til en dato-helper før bruk — navnet lyver om halvåpne intervaller. Merk: `hentForelderUkerapport` i `src/lib/forelder.ts` har samme feil fra før (`lte: ukeSlutt`) og bør ryddes

2026-08-15 | Digesten krasjet med «Cannot read properties of undefined (reading 'findUnique')» rett etter at ny tabell var opprettet og `prisma generate` kjørt — så ut som feil i DDL-en eller modellnavnet | Dev-serveren sto og kjørte med den gamle Prisma-klienten. Dette er den dokumenterte gotchaen «Dev-server med foreldet Prisma-klient etter `prisma generate`» — jeg leste den ikke før jeg feilsøkte | RESTART dev-serveren etter hver `prisma generate`, før du feilsøker noe som helst som ser ut som en manglende modell. Sjekk gotchas-lista FØR feilsøking, ikke etter

2026-08-14 | Satte `--accent` til Paper `--p-soft` etter shadcn-konvensjonen «dempet hover-flate». Målt i nettleseren ga det 1,19:1 på tre tekstelementer på `/stats/*` — usynlig tekst | Jeg mappet tokenet etter hva shadcn MENER med navnet, ikke etter hva denne koden faktisk gjør med det: `--accent` leses som tekstfarge 93 steder (eyebrow, kursiv em) i tillegg til 183 flater. I Presis var den merkevare-høydepunktet, ikke en hover-flate | Før du bytter verdien på et delt token: tell konsumentene og se hvilke ROLLER de gir det (tekst vs. flate vs. kant). Et navn som følger en kjent konvensjon betyr ikke at koden bruker det slik. Og: kjør alltid en kontrastmåling over hele DOM-en etter en token-endring — en `verify` som er grønn sier ingenting om lesbarhet

2026-08-14 | Designport steg 10: erklærte «alle Presis-farger ute av src/» etter at hex-grepen var 0, men nettleseren viste fortsatt neon-lime på `/stats/sg-sammenlign` | Ferdig-definisjonen målte kun `#005840`/`#D1F843` som hex. Nøyaktig de samme fargene lever i to notasjoner til — som `rgb()`-desimaler (`rgba(209,248,67,…)`, `rgba(0,88,64,…)`, 189 forekomster) og som shadcn hsl-tripletter (`72.9 92.8% 61.8%`, `163.6 100% 17.3%`, 3 forekomster i `marketing/v2/stats-ramme.tsx`). En hex-grep kan ikke se noen av dem | Når en farge skal fjernes, søk etter FARGEN i alle notasjoner den kan skrives i (hex, `rgb()`/`rgba()`-desimal, hsl-triplett, `color-mix`-argument) — ikke etter strengen. Konverter verdien til rgb først og grep på tallene. Gjelder enhver «fjern farge X»-oppgave i dette repoet
2026-08-14 | Kvalitetsløft-økta måtte legge om del 2 midtveis: oppgaven ba om å slette tre `(legacy)`-mapper som «død kode», men 53 av de 76 sidene der er eneste leverandør av URLer som levende navigasjon peker på (`/admin/stall`, `/admin/kalender/uke`, hele `/portal/mal/sg-hub`) | Den eksterne auditen talte filer og linjer, ikke rutbarhet. `(legacy)` er en RUTEGRUPPE — parentesnavnet forsvinner fra URLen, så en side der er like live som en utenfor. En ren import-analyse viser dem som «ubrukte» fordi ruter nås via URL, ikke via import | Før du kaller ruter døde: mål rutbarhet, ikke bare imports. To sjekker — (1) finnes URLen også utenfor rutegruppen? (2) lenker levende kode til den? Og sjekk om siden er en ren `redirect()`-stubb: 17 av de 25 slettbare var det, og en naiv sletting hadde gjort fungerende videresendinger til 404. De hører hjemme i `next.config.ts`, ikke som ruter

2026-08-14 | `npm run verify` feilet med 8 «Cannot find module .../(legacy)/.../page.js» rett etter filsletting, og så ut som en ekte importfeil | `.next/types/validator.ts` er GENERERT og lå igjen fra forrige build med referanser til de slettede rutene | Slett `.next` før verify når du har fjernet eller flyttet rutefiler — ellers feilsøker du en stale generert fil

2026-08-11 | Enhetstesten `restitusjonsstatus.test.ts` feilet med «$Class.getPrismaClientClass is not a function» og kostet én rekjøring | `npm test` og `npm run verify` kjørte parallelt i samme checkout — verifys `prisma generate` skrev om `src/generated/prisma` mens testen lastet den | Kjør aldri `npm test` parallelt med `npm run verify` i samme checkout — verify inkluderer generate; kjør dem sekvensielt eller la verify fullføre først

2026-08-11 | Tre parallelle `npm run verify`-kjøringer kolliderte (delt `.next`-lås → «Another next build process is already running», og eslint leste `src/generated` midt i en regenerering) fordi bakgrunnsoppgavens «completed»-varsel kom lenge før prosessen faktisk var ferdig — også Monitor-varsler rapporterte funn av exit-markøren mens filen beviselig ikke inneholdt den | Fullførings-varsler for bakgrunnsoppgaver i denne agent-miljøvarianten er upålitelige; jeg startet ny kjøring i god tro hver gang «completed exit 0» kom | Stol ALDRI på et fullførings-varsel alene for langkjørende bakgrunnskommandoer: bevis = din egen `grep` etter exit-markøren i loggfilen (+ `pgrep` på prosessen). Én verify om gangen — sjekk `pgrep -f "npm run verify\|next build"` FØR du starter en ny

2026-08-10 | Fire PR-er (#386–#389) ble bygget og merget med rød Vercel-preview uten at noen leste feilen; skjermbilde-gaten sto i praksis stille fordi ingen preview kjørte | `postinstall: prisma generate` laster `prisma.config.ts`, som kaster på `env("DIRECT_URL")`. Variabelen er satt for Production, men ikke for Preview — sannsynligvis siden secret-rotasjonen 05.08 kun traff production. Prod bygde derfor fint, og lokal `npm run verify` + CI (som begge har egne env) var grønne, så ingen av gatene kunne se det | Rød Vercel-sjekk på en PR skal leses, ikke antas å være støy. Og: skjermbilde-gaten kjøres mot PREVIEWEN, aldri mot localhost — localhost beviser bare at skjermen ser riktig ut på maskinen som kjørte den. Full runbook lå i «VERCEL-PREVIEW-FEIL-2026-08-10.md», slettet i opprydding 17.08.2026 — git-historikk

2026-08-10 | Konkluderte med at «databasen er ikke tilgjengelig på MacBook Air» og bygde hele arbeidsflyten for PP-1.7 rundt en Vercel-preview (env-endring, tre redeploys) i stedet for lokal dev. Databasen virket hele tiden | Prøven min var en `tsx`-engangsfil som importerte `src/lib/prisma` UTEN å laste `.env.local` først. `prisma.config.ts` gjør det kun for CLI-en, så `DATABASE_URL` var undefined og adapteren falt tilbake til localhost:5432 → `ECONNREFUSED`. Jeg leste feilkoden som «nettverket/nøklene er døde», og et gammelt statusnotat om sensurerte verdier bekreftet det jeg allerede trodde | `ECONNREFUSED` mot en Supabase-URL er selvmotsigende — pooleren avviser ikke, den svarer eller tidsavbryter. Ser du den koden: sjekk FØRST om env faktisk er lastet (skriv ut vertsnavnet, aldri nøkkelen), deretter DNS/TCP mot verten. Engangsskript må laste `.env.local` selv med `dotenv` — importen av `src/lib/prisma` gjør det ikke for deg

2026-08-10 | `main` var rød med 114 feil (83 tsc, 17 eslint, 14 token-gap) — hele nattkjøringen måtte starte med opprydding i stedet for skjermarbeid, og dev-serveren rendret blanke sider | Automatisk slug-/wave-tagging skrev ugyldig kode (uavsluttet wrapper-div, `*/` inne i blokk-kommentar, duplikate objektnøkler) og ble merget uten at `npm run verify` var kjørt. Syntaksfeilene fikk tsc til å hoppe over semantisk sjekk, som skjulte 79 videre feil — og tsc-fallet skjulte i sin tur eslint og token-gaten. To nye gater (lime-lint i PP-0, token-gap) ble dessuten innført uten å rydde eksisterende brudd | Kjør ALLTID `npm run verify` etter en automatisk kodemod over mange filer — kodemodden kan produsere kode som ser riktig ut i diffen. Og: én grønn kjøring beviser bare det ØVERSTE laget; innfører du en ny gate, rydd eksisterende brudd i samme PR, ellers står main rød uten at noen ser det

2026-08-06 | Pollet `mcp__github__actions_list` gjentatte ganger under PR-overvåking (PR #302); hvert kall returnerte full repo-metadata uansett `minimal_output: true` | Antok webhook-abonnement + egen statussjekk var billig, uten å vite at `actions_list` ikke trimmer responsen | Gotcha lagt i `.claude/rules/gotchas.md`: stol på `<github-webhook-activity>`-hendelser fremfor å polle; bruk `pull_request_read(get_status)` (lett) hvis manuell sjekk trengs

2026-08-07 | `mcp__github__actions_list` (`list_workflow_runs`) sprengte kontekstgrensen (415k tegn) selv med `per_page: 5` — parameteren ble ignorert, 30 kjøringer returnert | Kjørte kallet selv om gotcha-en fra 06.08 allerede advarte mot det; det finnes ingen lett variant for «siste workflow-kjøring» | Trenger du kjøringsoversikt: la kallet feile til fil og parse den med `python3 -c "json.loads(...)"`, eller bruk `pull_request_read(get_check_runs)` per PR — aldri les `actions_list`-responsen rått

2026-08-15 | Testdekningsanalyse: to tall/påstander i planutkastet var feil og måtte rettes før commit — «40+ udekkede filer med rå datomatte» (faktisk 143) og en henvisning til `coach-scope-idor.test.ts` som mønster for Prisma-mocking (den mocker ikke Prisma i det hele tatt) | Begge kom av å lese en TRUNKERT kommandooutput som om den var komplett: `head -40` ga 40 treff og jeg skrev «40+», og en `grep -l "mock.module"`-liste jeg aldri kjørte mot akkurat den filen. Ingen av dem ville blitt fanget av `verify` — en plan er prosa, ingen gate leser den | Tall som skal inn i et dokument telles med `wc -l`/en teller, aldri fra en liste du har begrenset med `head`. Og: siterer du en fil som MØNSTER for noe, grep i akkurat den filen etter akkurat det mønsteret først — «den ligger i riktig mappe» er ikke verifikasjon

2026-08-15 | Steg 3 av testdekningsplanen: de aller første enhetstestene på `src/lib/sg-hub/` avdekket to feil som hadde ligget ute i prod — alle fem wedger klassifisert som «wood» (`classifyClub` sjekket `endsWith("w")` før wedge-lista, så hele wedge-grenen var død kode), og NaN som lakk helt ut i smash-grafen når alle slag hadde samme kølle-fart | Ingen av dem kunne fanges av `tsc`, eslint eller skjermbilde-gaten: begge produserer tall som SER plausible ut. Wedge-feilen ga for kort carry, for lav apex, og fjernet «stop-on-green»-bonusen i `same-distance-strategy` — altså feil kølleanbefaling til spilleren, ikke bare et skjevt tall | Ren beregningskode som produserer spiller-synlige tall skal ha enhetstester før den tas i bruk — den er den ENESTE feilklassen i dette repoet der ingen annen gate kan se feilen. Og: en `endsWith`/prefiks-fallback må alltid stå SIST i en klassifiseringskjede; står den foran de eksakte treffene, blir grenene under den stille død kode
- 2026-08-20 — Leste CLAUDE.md §Hooks som fasit på hva som er sperret, og konkluderte at sletting av remote-grener var `deny`. Rapporterte til Anders at han måtte rydde 75 fjerngrener manuelt i GitHub-UI-et. `beskytt.mjs` hadde i virkeligheten `--delete` i `askMønstre`, ikke i deny-listen; kommandoen gikk gjennom på første forsøk da den faktisk ble prøvd. Lærdom: verifiser en sperre ved å prøve den (eller lese hooken), aldri ved å lese en oppsummering av den. CLAUDE.md-linja er rettet.
- 2026-08-25: lint-staged-porten kjører `tsc --noEmit` for HELE prosjektet når én .ts stages — i en fersk worktree feiler den til `npx prisma generate` (dummy DIRECT_URL/DATABASE_URL, aldri kopier .env) er kjørt. OBS: tsconfig kompilerer også .ts under docs/ — flytter du en docs-spec-fil, må relative imports rettes (workbench/store/actions.ts → arkiv/domain).
- 2026-08-25: D2 (Train-lock-tokens) ble bygget TO ganger parallelt — PR #586 (merget til main 11:16 UTC) og PR #587 (min, åpnet 11:15 UTC), begge nye filer på nøyaktig `src/styles/train-lock-tokens.css` + `src/lib/v2/train-lock.ts`. Ingen av sesjonene visste om den andre; jeg oppdaget det først da PR-en min ble `mergeable_state: dirty`. Kostnad: hele min implementasjon kastet. Lærdom: **`git fetch origin main` + sjekk `log HEAD..origin/main` FØR arbeidet starter på en beslutningsgate, ikke bare før push** — en gate som står som «åpen» i planen kan være tatt av en parallell økt for minutter siden. Sidefunn: #586 rørte ikke CLAUDE.md/beslutninger.md/LAUNCH-PLAN, så alle tre påsto fortsatt at tokens ikke fantes i `src/` — presedens-drift av den farligste sorten (planen §1a), rettet i PR #587.
- 2026-08-25: tredje gang samme dag at TILSTANDEN var foran styringsdokumentene. S1 (RLS i prod) sto som «venter på Anders' ja» og §0 sa «IKKE kjørt mot prod» — men RLS var på begge workbench-tabellene med 6 policies, og migrasjonsrecord + DONE-fil hadde ligget på main siden #583. `RLS-WORKBENCH-DONE.md` påsto i tillegg om seg selv «Ikke kjørt mot prod ennå. Ikke merget til main» mens den lå på main. Lærdom: **verifiser tilstanden i systemet FØR du kjører en gate, ikke bare planen som beskriver den** — for DB-gater betyr det et read-only oppslag (`relrowsecurity`, `pg_policies`) før noe DDL. Kostet null her fordi sjekken kom først, men to tidligere runder samme dag kostet en hel implementasjon. Mønsteret: en DONE-fil som beskriver sin egen fremtid («skal kjøres») blir aldri oppdatert av den som kjører den.
- 2026-08-26: `check-critical-imports.mjs` feilet «BUNDLE FAIL … undefined» på alle 5 kritiske entries — så ut som min PortalChatHjem-endring hadde brutt bundlingen. Rotårsak: worktreet hadde INGEN node_modules i det hele tatt (tsc/eslint hadde likevel kjørt grønt, siden Node sin modul-resolusjon vandrer opp til hovedrepoets node_modules for imports — men esbuild-scriptet leser binæren via path.join(cwd, "node_modules", …), som ikke fantes lokalt). Fiks: `npm ci` i worktreet (kjent gotcha, se gotchas.md «worktree-build-krever-npm-ci» — denne gangen var symptomet en falsk BUNDLE FAIL i stedet for Turbopack-feilen der, samme rotårsak).
- 2026-08-27 (T9): Byggeordren sa «port /admin/agencyos/live til AG-09/AG-09b Live-tavle» — men koden bak den ruten («Mission Control») var slett ikke pågående treningsøkter, det var en statisk seed-mockup av Anders' PERSONLIGE innboks (e-poster, meldinger, kalender, Notion-prosjekt), et helt annet konsept enn fasitens timer-kort. Ingen gate (tsc/eslint/build) kunne ha fanget dette — det er en riktig kompilerende side som bare viser feil ting. Lærdom: når en byggeordre navngir en fasit for en eksisterende rute, les FAKTISK INNHOLD i den ruten (ikke bare filnavn/kommentarer) før du antar fasit og kode beskriver samme feature — stoppet og spurte Anders før noe ble bygget videre, byttet til ekte `trainingSessionV2`-spørring i stedet.
- 2026-08-27 (PR #627, C4): pushet tre commits i ett `git push` der SISTE commit kun var en DONE-rapport (`natt/LOOP-C4-DONE.md`). Vercel-preview ble likevel `Ignored`/`Canceled by Ignored Build Step`, selv om de to foregående commitene endret ekte kode (nye komponenter, wiret om en rute). Rotårsak: `vercel.json`s `ignoreCommand` er `git diff --quiet HEAD^ HEAD -- ':!*.md' …` — den evaluerer KUN tuppen sin commit mot SIN EGEN forelder, aldri hele push-området mot forrige HEAD på fjernbranchen. En doc-only commit til slutt i en ellers kodetung PR slår derfor av HELE PR-ens forhåndsvisning, uansett hva de tidligere commitene inneholder. Ingen `redeploy`-verktøy fantes i denne øktens Vercel-MCP for å tvinge en ny bygging uten en ny, ekte push. Lærdom: **skal en DONE-/leveranserapport committes i samme PR som koden, ikke la den stå som SISTE commit hvis PR-en trenger en Vercel-preview** — commit rapporten FØR siste kodeendring, eller vent med den til etter at forhåndsvisningen er bekreftet bygget. `ignoreCommand`-mønsteret her (`HEAD^..HEAD`) er en kjent Vercel-begrensning, ikke noe som er spesifikt rettet for multi-commit-push i dette repoet.
- 2026-08-27 (statusmåling): `scripts/maal-trainlock-status.mjs` har hardkodet `ROOT = ~/Developer/akgolf-hq` (linje 9) og måler ALLTID hovedmappen — uansett cwd. Hovedmappen sto på `feat/workbench-b6-godta-avvis` (13 commits bak origin/main), så rå kjøring ga 99 PAPER / 8 PORTET mens fasiten på origin/main var 77 / 45. Kostnad: en agent måtte oppdage avviket og re-kjøre mot `git archive` av origin/main i scratchpad. Lærdom: sjekk `git -C ~/Developer/akgolf-hq branch --show-current` FØR du siterer scriptets tall — eller send inn ROOT som argument (scriptet bør fikses til å ta rot fra cwd/argv).
- 2026-08-29: Skjult Browser-pane gir innerWidth=0 og suspendert rAF — scroll-animasjoner kan ikke verifiseres der (blanke screenshots, stale CSS-vars). Kostet ~30 min feilsøk; bruk Playwright headless mot dev-serveren i stedet.
- 2026-08-29 (PX-4): RU-01/RU-01L-fasiten («Live runde iPhone») tegner en helt annen datamodell enn koden — score-stepper (+/−) med FW-TREFF/GIR/2-PUTT-piller, mens `hull-foring.tsx` bruker en slag-for-slag-kjede (lie+avstand per slag) som eneste SG-motor i repoet. Ingen FW/GIR-konsept finnes i `src/lib/runde-logg/` i det hele tatt (grep null treff). Å bygge fasitens forenklede stepper ville krevd en ny, parallell scoring-datamodell — utenfor pikselport-scope (anti-scope: ingen ny datamodell). Skippet med vilje, notert i PR i stedet for å bygges rundt.
- 2026-08-29 (PX-4): LO-familien har to blokkere, ikke pikselarbeid. LO-01/LO-01L («Gate») tegner en 4-flate produktvelger (Player HQ/Agency OS/Club OS/Forelder) som IKKE finnes i koden — `etter-innlogging/page.tsx` redirecter rollebasert direkte, ingen manuell velger — å bygge den er ny IA/funksjonalitet, ikke port. LO-02 («Innlogging») tegner et MØRKT loginskjema, men `.claude/rules/beslutninger.md` §A4 (16.08.2026, FAST REGEL) låser /auth til LYS — usikkert om Train-lock (25.08) er ment å overstyre denne spesifikke låsen eller ikke. Begge notert i PR-en for Anders' avgjørelse i stedet for å gjettes på.
- 2026-08-29 (PX-4): P-familien (Player Mac/iPhone uke/økt/måned/årsplan) og resten av WB-familien deler samme motor som AgencyOS-workbenchen (`WorkbenchV2.tsx` 3300 linjer + `WeekGrid.tsx`, allerede pikselportet i PX-2 mot A-familien) — ikke separate skjermer. Fant én reell avviks-kandidat verdt å flagge, IKKE rettet: P-01 sier eksplisitt «ingen Publiser» for spilleren, men `WorkbenchV2.tsx` linje ~2099-2104 viser Publiser-knappen til spiller-rollen også (kun uten «til [navn]»-suffiks) — dette kan være tilsiktet selvbetjening for spillere uten coach, IKKE en bug; endret ikke uten Anders' ja, siden fjerning av en eksisterende handling er funksjonsendring, ikke pikselarbeid. Kunne heller ikke verifisere visuelt (worktree har dummy DB — ingen ekte innlogging/skjermbilde mulig lokalt, se gotchas.md om worktree+.env), så unnlot å gjøre risikable endringer i en komponent som deles av alle Workbench-skjermer i begge produktene uten å kunne se resultatet.
- 2026-08-29 (PX-4, andre økt samme dag): `git push -u origin px/4-player-wb-runde` feilet 403/rejected — branchen var IKKE ny, en tidligere økt (annen session-id) hadde allerede pushet PX-4-arbeid (RU-02+WB-00) til nøyaktig samme branch-navn og åpnet PR #657 (draft) FØR denne økten startet, selv om oppdraget ba om å opprette grenen fra fersk `origin/main`. `git ls-remote origin <branch>` avslørte kollisjonen umiddelbart — ikke stol på at et branch-navn gitt i oppdraget faktisk er ledig, sjekk alltid `git ls-remote` (eller `git fetch` + `git log <branch>..origin/<branch>`) FØR push, ikke bare før `git checkout -b`. Løst med `git rebase origin/<branch>` + manuell reconciliation av 3 filer med ekte konflikt (2 dokfiler + `runde-recap.tsx`, alle tre var «begge økter la til samme type innhold på samme sted») — ingen av øktenes arbeid kastet, kombinert PR-beskrivelse skrevet.
- 2026-08-29 (PX-3): denne remote-økten hadde INGEN `.env.local`, ingen reelle Supabase/DB-credentials og ingen tilgang til `SCREENTEST_PASSWORD` (Supabase-MCP-en krevde auth denne økten ikke hadde). `scripts/app-shot.mjs` krever ekte innlogging mot Supabase Auth — det finnes ingen lokal/offline auth-vei i appen. Skjermbilde-gaten (04.08, fast regel) kunne derfor IKKE oppfylles i denne økten uansett hvor mye koden ble pikselporter — `npm run verify` (med midlertidige dummy `DIRECT_URL`/`DATABASE_URL` KUN i shell-miljøet, aldri i fil, jf. gotcha «Aldri kopier .env* inn i en worktree») dekker typecheck/lint/build/tester, men aldri visuell verifikasjon. Lærdom: en økt som mangler `.env.local` bør sjekke DETTE FØRST (før dyp skjermporting) og varsle brukeren tidlig, ikke oppdage det etter timevis med kode — neste PX-økt med ekte credentials må ta skjermbildene for denne PR-en før merge.
- 2026-08-29 (PX-3): TM-fasit-familien har to generasjoner av SAMME skjerm uten at filnavnet sier det — TM-02/TM-05 («TrackMan økt» / «Tom og faa slag») tegner spredningskartet på et BLANKT rutenett, mens TM-07/TM-08/TM-08f/TM-09 (høyere nummer, senere i sekvensen) tegner SAMME skjerm med et dekorativt hullkart (rough/fairway/green/bunker) bak prikkene — TM-07s egen tekst («Spredning ekkes alltid på hull … aldri blankt koordinatsystem») og TM-08fs kommentar («Rekkefølgen på TM-08 … sto allerede riktig») bekrefter at hullkartet er den GJELDENDE retningen, ikke et alternativ. Lærdom: når to `.dc.html`-filer beskriver visuelt ULIKE versjoner av samme skjerm-konsept, stol på det HØYESTE nummeret i samme familie (senere = nyere revisjon) og sjekk HANDOFF.md for eksplisitt bekreftelse — ikke porter den eldre varianten bare fordi den også står i oppdragets filliste.
- 2026-08-29 (PX-3-rest): TM-12/TM-13/TM-14 er IKKE porterbare som rene pikselporter — alle tre tegner funksjonalitet som ikke finnes i datamodellen: TM-12 («Økt teknikk og slag») krever coach-satte live-målvinduer per TrackMan-parameter (Club Path/Face Angle) med per-slag in/ut-av-vindu-klassifisering; TM-13 («Progresjon målvindu») krever ukentlig aggregering av spredning mot et målvindu med bruddpunkt-deteksjon koblet til utstyrslogg + Jarvis-varsel; TM-14 («Bag mapping og DECADE») krever en «DECADE-kjegle»-visualisering og bag-mapping-konsept som ikke finnes i koden (`grep -ri "DECADE\|bag.mapping"` i src/ gir null treff utenom en urelatert artikkel-fil). Ingen av de tre kan bygges uten ny datamodell/forretningslogikk — utenfor anti-scope («ingen nye features/datamodell-endringer»). TM-03 (ingest-tilstander) samme konklusjon for busy/feil-tilstandene: PDF/foto-OCR-ingest finnes ikke (kjent gap fra 29.08 over), og CSV-ingest i `TrackmanImportModal` er synkron (parse skjer i nettleseren på fil-valg) — det finnes ingen asynkron «Leser og analyserer…»-busy-tilstand å porte. Lærdom: sjekk om skjermens KJERNEHANDLING (ikke bare layout) finnes i domenet FØR du åpner fasitfilen for porting — spar tid ved å grep'e etter kjernebegrepet (DECADE, målvindu, bag mapping) før du leser hele HTML-en.
- 2026-08-29 (PX-3-rest): `ScoringDetailsSchema.perSlag.verdier` (test-scoring.ts) tillot kun `number | boolean | null`, men `scoreTest()` (samme fil) skriver faktisk `Verdi = number | boolean | "V" | "H" | null` til nøyaktig samme felt for Gate-protokoller med `miss_side` (Putt Gate m.fl. — se `harMissSideFelt`/`tester-live.ts`). Ethvert lagret `TestResult.details` fra en Gate-test med en registrert BOM+retning ville dermed FEILE sin egen les-validering (`z.record` sjekker ALLE verdier i objektet — én ugyldig streng feiler hele perSlag-raden). Oppdaget da TE-03-portens «Siste forsøk»-rutenett (leser `details` for å vise OK/BOM · V|H) kom tomt tilbake på testdata med miss_side. Fikset ved å utvide unionen med `z.enum(["V","H"])` — additivt, ingen eksisterende konsument innsnevres (kun to konsumenter fantes, begge i test-scoring.ts selv). Lærdom: når en skrive-side (`Verdi`) og en lese-side (`ScoringDetailsSchema`) beskriver «samme» felt i to separate typer i samme fil, sjekk at unionene faktisk stemmer overens — de kan drifte fra hverandre uten at noen tsc-feil varsler det (begge er gyldig TypeScript hver for seg).
- 2026-08-29 (PX-6): samme worktree-uten-node_modules-gotcha som 26.08 (`check-critical-imports.mjs` BUNDLE FAIL, esbuild-binæren mangler lokalt). Denne gangen prøvde jeg en snarvei — `ln -s <hovedrepo>/node_modules node_modules` i worktreet — for å slippe `npm ci` (som ikke er tillatt/ønsket i en agent-worktree). Symlinken fikk `check-critical-imports` til å passere, men fikk `next build` (Turbopack) til å FEILE HARDT: «Symlink [project]/node_modules is invalid, it points out of the filesystem root» — Turbopack nekter en node_modules-symlink som peker utenfor worktreets egen filsystem-rot. Lærdom: **ikke symlink node_modules inn i en worktree** — fjern symlinken igjen med en gang (`rm node_modules`, IKKE `rm -rf` siden target er delt). `tsc --noEmit`, `eslint --quiet src` og `npm test` kjører fint UTEN node_modules i worktreet (Node sin modul-resolusjon vandrer opp til hovedrepoet for vanlige imports) — kun de to esbuild/Turbopack-avhengige stegene (`check-critical-imports`, `npm run build`) er reelt blokkert i en worktree, og det er forventet (se `.claude/rules/gotchas.md` «Aldri kopier .env*» / PR #275-mønsteret).
- 2026-08-29 (PX-7, fersk CCR-container): `npx tsc --noEmit` ga 58 000+ feillinjer («Cannot find module 'next'» osv.) — så ut som hele prosjektet var ødelagt. Rotårsak: `node_modules/` fantes ikke i det hele tatt (container uten `npm ci` kjørt ennå), ikke et reelt typefeil. `npm ci` (inkl. `prisma generate` via postinstall) fjernet alle unntatt 2 ekte feil. Sjekk `ls node_modules` FØR du tolker en skyhøy tsc-feilliste som reell kodeskade.
- 2026-08-29 (PX-7): samme CCR-container manglet `DATABASE_URL`/Supabase-nøkler → `next dev` feiler i `instrumentation.ts` (`validateEnv`) ved oppstart, ingen innlogget rendering mulig. Skjermbilde-gaten (CLAUDE.md, FAST REGEL 04.08) kunne derfor ikke gjennomføres av agenten selv denne økten — B2/B3/B4-arbeid som krever visuell verifikasjon ble dokumentert som spec i stedet for bygget blindt (se `natt/PX7-DONE.md`). Neste økt som trenger levende rendering bør bekrefte `.env.local` har ekte Supabase-verdier FØR den lover skjermbilder.
- 2026-08-29 (PX-7, MAT-audit): `StatusPill`s `tone="up"` rendret `TL.ok` (grønn) — og «Fullført»-status i `domene.tsx`/`domene2.tsx`/`AdminHandlingssenterV2.tsx` brukte nettopp den tonen, i strid med MAT-00/CLAUDE.md invariant 2 («Fullført er ALDRI grønn — TL.ok kun Godta/PUBLISERT»). Ingen gate (tsc/eslint/test) fanger dette — det er en fargesemantikk-regel, ikke en typefeil. Funnet ved grep etter `Fullført`+`TL.ok` i samme fil, ikke ved lesing av én skjerm om gangen. Rettet med ny tone `"warm"`.
- 2026-08-29 (Vercel-kostnadsundersøkelse): Anders rapporterte at «hver push trigger fullt bygg selv ved doc-only-commits», ~$500/mnd, 20 deployments/12t med flere `cancelled`/`failed`. Ingen del av premisset stemte ved verifisering. `vercel.json`s `ignoreCommand` fantes allerede på `main` (identisk med det jeg ble bedt om å lage) og virker riktig — testet direkte mot de to doc-only-commitene i vinduet (`git diff --quiet HEAD^ HEAD -- ':!*.md' ':!docs/' ':!.claude/' ':!designsystem/' ':!kart/'` ga exit 0 for begge). `mcp__vercel__list_deployments` viste 4 `CANCELED` + 1 `ERROR` av 20 — men `get_deployment` på hver enkelt viste at alle 4 `CANCELED` hadde `errorLink` som peker til Vercels «ignored-build-step»-dokumentasjon og kun 13–19s mellom `buildingAt` og `ready`: korrekt hoppet over, ikke et avbrutt ekte bygg. `ERROR`-en var en forbigående Vercel-feil («duplisert cron-jobb», `betalings-purring`) på en forhåndsvisning — filen har kun én forekomst av jobben, neste bygg av samme gren var grønt uendret. De 20 byggene var seks parallelle nattlige PX-piksel-porting-økter × forhåndsvisning+produksjonsbygg per PR — reelt, villet volum. Lærdom: en `CANCELED`-status i Vercels deployment-API dekker BÅDE «riktig ignorert via ignoreCommand» og «avbrutt av nyere push på samme gren» — de skilles KUN ved å hente full detalj per deployment (`errorLink` + `buildingAt`→`ready`-varighet i sekunder vs. minutter), aldri av listevisningen alene. Før en rapportert Vercel-kostnad tolkes som feilkonfigurasjon: hent faktisk deployment-historikk og verifiser ignoreCommand mot de faktiske commit-SHA-ene FØR noe i `vercel.json` endres — kostnaden her var ekte, villet utviklingsvolum, ikke en bug.
- 2026-09-02 (sen kveld, Ø14/Ø15): tok Ø14/Ø15 fra MASTERPLAN slik den så ut ved øktstart — den parallelle worktree-økten hadde allerede levert begge i PR #754 to timer før. Oppdaget først som flettekonflikt. Arbeidet ga likevel nytt: sign-off-riggen avslørte at demo-stallen i prod var TOM for coachtest (37 spillere uten `PlayerEnrollment`), noe skjermbilde-runden i #754 hadde tolket som «basen nullstilt». Lærdom: `git show origin/main:docs/MASTERPLAN-GJENSTAAENDE.md | grep "^| Ø14"` FØR en rad startes, og «ingen data» i en liste er et funn som skal forklares, ikke en tilstand som skal godtas. Ingen skill fanget det — det er en økt-disiplin, lagt i auto-memory.
- 2026-09-02 (sen kveld, 15.13): `gh pr merge` svarte «Merge already in progress» på #754 og `gh pr merge --delete-branch` på #755 ga exit 1 — begge var faktisk merget da jeg sjekket 15 s senere. Ikke retry på feilmeldingen; sjekk `gh pr view --json state` først.
- 2026-09-05 (prosjektrevisjon): STATUS-NÅ 03.09 påsto «`TallHero` slutter å telle opp (`useCountUp` fjernet)» og MASTERPLAN 19.6/19.7 sto som «I ARBEID (parallell økt, gren …)» — grep i main viste `useCountUp` fortsatt på `core.tsx:284`, og grenen fantes kun lokalt i hovedmappa med én upushet commit (03.09 02:24) + 150 ukommitterte filer. Linjen var skrevet fra beslutnings-PR-en (#765, docs) som om den var kode-PR-en | Statuslinjer ble skrevet fra PR-tittel/-beskrivelse, ikke fra `git log origin/main` + grep i `src/` — og en parallell økt i hovedmappa avsluttet uten push (bryter «alt skal være pushet ved sesjonsslutt») | Før en STATUS-/MASTERPLAN-linje sier «levert»: (1) `git log origin/main --oneline --grep=<nøkkel>` skal treffe en kode-commit, ikke bare en docs-commit, (2) grep i `src/` skal bekrefte at symbolet er borte/inne. Og: `git worktree list` + `git branch --list` for å finne strandede grener hører med i enhver statusgjennomgang. Ingen skill fanget det — `verify-og-commit` kjører ikke ved øktslutt uten at økten selv kaller den
