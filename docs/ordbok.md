# Ordbok og språk

Språk, begreper og treningsplanlegging ligger samlet i én master:
**[Treningsplanlegging og språk (MASTER)](treningsplanlegging.md)**.

Denne fila er bare en peker, slik at eksisterende lenker virker. Den er ikke en egen fasit.
`docs/ordbok.json` er avledet data generert av `scripts/ordbok-json.ts`.
