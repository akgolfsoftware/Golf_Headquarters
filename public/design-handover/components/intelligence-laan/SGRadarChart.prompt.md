5-axis radar (OTT, APP, ARG, PUTT, Consistency) — player as a solid brand-filled polygon, PGA Tour benchmark as a dashed grey outline.

```jsx
<SGRadarChart player={[0.8, 0.65, 0.4, 0.55, 0.7]} benchmark={[0.5, 0.5, 0.5, 0.5, 0.5]} />
```

Pass values pre-normalised (0–1) — map your raw SG range to 0–1 before calling.
