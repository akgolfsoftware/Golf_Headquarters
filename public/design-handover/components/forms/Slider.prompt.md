# Slider

Rekkevidde-kontroll. Lime-fylt track, tilpasset tommel.

## Bruk
```jsx
<Slider label="Intensitet" value={80} min={0} max={100}
  onChange={setIntensitet} formatValue={(v) => `${v}%`} />
```
